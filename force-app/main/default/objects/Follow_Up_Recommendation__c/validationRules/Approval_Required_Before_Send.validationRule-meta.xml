<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Approval_Required_Before_Send</fullName>
    <active>true</active>
    <description>Human-in-the-loop backstop: a client or provider follow-up can never be Sent without an approval stamp, whoever writes the record (UI, API, integration, data load).</description>
    <errorConditionFormula>AND(
    ISPICKVAL(Status__c, &quot;Sent&quot;),
    NOT(ISPICKVAL(Audience__c, &quot;Internal&quot;)),
    OR(ISBLANK(Approved_By__c), ISBLANK(Approved_Date__c))
)</errorConditionFormula>
    <errorMessage>Client and provider follow-ups must be approved before they can be marked Sent.</errorMessage>
</ValidationRule>
