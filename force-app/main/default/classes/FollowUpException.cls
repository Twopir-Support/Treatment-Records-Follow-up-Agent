/**
 * Business-rule violation in the Treatment & Records Follow-up workflow (permission, status, validation).
 * Messages are user-facing (Custom Labels) and safe to surface in the UI and to the agent.
 */
public with sharing class FollowUpException extends Exception {
}
